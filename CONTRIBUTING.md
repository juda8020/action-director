# 貢獻與更新規則

Action Director 採 MIT License，歡迎回報問題、改善文件及提交程式修改。

## 每次更新的完成條件

任何會修改專案檔案的工作，在宣告完成前必須：

1. 在 `CHANGELOG.md` 的「尚未發布」加入本次變更。
2. 使用最適合的分類：新增、變更、修正、文件、測試、內部或安全性。
3. 描述使用者可觀察到的結果，不只列出檔名或函式名稱。
4. 若改變介面能力、支援格式或產品限制，同步更新 README、程式內教學與
   對應語言手冊。
5. 若改變 ActionSpec 或 Runtime 契約，同步更新 Schema 文件與測試。
6. 執行 `./tests/run-tests.sh`，把實際結果寫進更新紀錄；不能把未執行的
   測試寫成已通過。

修正同一項尚未發布功能時，更新既有條目即可，避免把微小來回修改寫成
大量重複項目。正式發布時，將「尚未發布」內容移到含版本號與日期的新章節，
再保留一個空的「尚未發布」章節供下一輪使用。
